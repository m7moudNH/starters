return (str.match(/[aeiou]/ig) || []).length; //count of matching strings i -> case insensitive . g -> globally(find all matches) .
tis function either return a char arr or null so to prevent null we use || [] so it returns an empty array if not found
then we use .lenght to get the count

